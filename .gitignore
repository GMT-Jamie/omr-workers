# 樣本圖與 OMR 輸出**永遠不進版控**。
#
# 這是一個公開的 AGPL repo，而拿來測試的樂譜幾乎都是有版權的（實測那份第一頁頁腳
# 就寫著「未經編者及原作者同意，請勿轉載」）。把圖放進來等於在公開散布那份樂譜，
# 而產出的 .musicxml 是它的衍生資料，同樣不行。
#
# 所以 smoke_test.sh 與 bench.sh 都**吃路徑參數**，樣本自己放。
samples/*
!samples/.gitkeep
*.musicxml
*.mxl
*_teaser.png

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# 本機工作區
work/
out/
*.log

# 內部工作筆記（交接表、里程碑、決定紀錄）。**這是公開 repo，那些東西不進版控。**
#
# 裡面有正式機的內部 IP、開發機的本機路徑、有版權樣本的檔名，以及還沒對外講的
# 工作進度。它是寫給下一個接手的人看的，不是寫給讀 README 的人看的。
#
# ⚠️ 它被 ignore 代表 `git clean -fdx` 會直接刪掉它，而且它不在任何備份裡。
notes/
